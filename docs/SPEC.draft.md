# hearth SPEC (draft, work in progress)

This is a placeholder. The real SPEC.md will land before any meaningful code does.

It will cover:

- The three verbs (Ingest / Query / Lint) and their precise signatures
- The ChannelAdapter interface (InboundMsg + DeliveryAck)
- The AgentRuntime interface (ACP)
- The Vault permissions enforcement (per the user's SCHEMA.md)
- The frontmatter contract + provenance fields
- Scope discipline — explicit list of "what hearth does NOT do"
- Storage / TTL / retention rules
- Multi-format ingest pipeline (PDF / Word / Excel / video / web)
